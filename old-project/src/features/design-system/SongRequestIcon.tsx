import { HandIcon } from "@radix-ui/react-icons";
import { JSX } from "react";

export const SongRequestIcon = (): JSX.Element => (
	<span className="mr-[0.2rem] mt-[0.1rem] text-[1.2rem]">
		♪<HandIcon className="mr-[0.2rem] inline" />
	</span>
);
