// import { chordNumbersMatchPositionNumbers } from "./chord-numbers-match-position-number";
// import { getChordNumbers } from "./get-chord-numbers";
// import { getPositionNumbers } from "./get-position-numbers";

describe("chordNumbersMatchPositionNumber", () => {
	test("matches", () => {
		// const chordNumbers = getChordNumbers("I-3-5", "C");
		// const positionNumbers = getPositionNumbers([0, 0, 0, 3], "G4-C4-E4-A4").map(
		// 	(positionNumber) => positionNumber % 12,
		// );
		// const match = chordNumbersMatchPositionNumbers(
		// 	chordNumbers,
		// 	positionNumbers,
		// );
		// console.log({ chordNumbers, positionNumbers, match });
	});
});
