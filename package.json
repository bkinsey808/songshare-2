{
  "name": "songshare-2",
  "version": "1.0.0",
  "description": "SongShare monorepo",
  "scripts": {
    "expo": "cd expo-app && npx expo start --web",
    "worker": "cd cloudflare-worker && npm run dev"
  },
  "private": true,
  "devDependencies": {
    "vite-plugin-pwa": "^1.0.2",
    "vite-plugin-svgr": "^4.3.0"
  }
}
